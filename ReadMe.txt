If you gonna compile and run my code in Linux (Ubuntu bash, wsl etc.),  use "make"     for make command.
If you gonna compile and run my code in Windows(PowerShell, cmd etc.),  use "make win" for make command.
-------------
I used in clone() method in main (HexGameTest)